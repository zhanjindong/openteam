#!/bin/bash
# 【主动压缩钩子】当对话过长或切换任务时，提醒 Claude 执行结构化压缩
# 触发时机: 可由 Notification 事件触发，或在 CLAUDE.md 中指引 Claude 主动执行

cat << 'EOF'
📦 [Strategic Compact] 建议执行上下文压缩，请按以下步骤操作:

1. 【核心锚点 L1 — 完整保留】
   - 当前任务目标是什么？
   - DESIGN.md 中已确认的方案要点
   - 本次会话修改了哪些文件？
   - TODO.md 当前进度到哪一步？
   - 有没有未解决的阻塞问题？

2. 【工作状态 L2 — 压缩为摘要】
   - 每个修改过的文件一句话总结改动
   - 讨论过的备选方案和最终选择理由
   - 调试过程的关键发现

3. 【过程细节 L3/L4 — 丢弃】
   - 文件读取的原始内容
   - 命令的完整输出
   - 中间讨论和已放弃的方案细节

4. 【持久化检查】
   - 有无关键发现需要写入 .claude/memory/ ？
   - DESIGN.md 和 TODO.md 是否是最新状态？

请输出结构化的压缩摘要后继续工作。
EOF
